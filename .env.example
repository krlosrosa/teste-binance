DATABASE_URL="file:./dev.db"
ENCRYPTION_KEY="generate-with-node_-e_console.log(require('crypto').randomBytes(32).toString('hex'))"
PORT=3001
# Uma origem (legado) ou várias separadas por vírgula (recomendado com API no servidor + front no localhost)
FRONTEND_ORIGIN="http://localhost:3000"
FRONTEND_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"
